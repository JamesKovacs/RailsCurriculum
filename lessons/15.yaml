---
steps: |

  In your handy-dandy terminal, try:

      $ git add .
      $ git commit -m "Added votes"
      $ git push heroku master
      $ heroku run rake db:migrate

goal: |

  We just made some awesome changes. Let's push them so our friends can
  play with it.

goal_title: Commit and push to heroku
explanation: |

  Now we can commit our changes, push them out and run the
  remote migration... By this time it should be fairly old hat.
